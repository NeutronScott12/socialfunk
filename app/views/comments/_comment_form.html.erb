<%= form_for([@micropost, @micropost.comments.build], remote: true) do |f| %>
	<%= render 'shared/errors', object: f.object %>
	<%= f.text_area :content, class: "form-control", placeholder: "Post A Reply" %>

	<%= f.submit "Post", class: "btn btn-default" %>
<% end %>