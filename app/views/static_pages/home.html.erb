<% provide(:title, "Home") %>
<h2>Does actaully work</h2>

<% if logged_in? %>
	<div class="row">
		<aside class="container-fluid col-md-4">
			<section class="user_info">
				<%= render 'shared/user_info' %>
			</section>
			<section class="micropost_form">
				<%= render 'shared/micropost_form' %>
			</section>
		</aside>
		<div class="col-md-8">
			<h3>Posts</h3>
			<%= render 'shared/feed' %>
		</div>
	</div>
<% end %>