<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>Social Funk Forum</h2>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<p><%= link_to "Create Category", new_forum_path %></p>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>Gaming</h2>
		<% @forum_gaming.each do |game| %>
			<%= link_to game.title, forum_path(game) %>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>Politics</h2>
		<% @forum_politics.each do |game| %>
			<% link_to game.title, forum_path(game) %>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>News</h2>
		<% @forum_news.each do |game| %>
			<% link_to game.title, forum_path(game) %>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>Movies</h2>
		<% @forum_movies.each do |game| %>
			<% link_to game.title, forum_path(game) %>
		<% end %>
	</div>
</div>

<div class="row">
	<div class="container-fluid col-md-8 col-md-offset-2">
		<h2>General</h2>
		<% @forum_general.each do |game| %>
			<% link_to game.title, forum_path(game) %>
		<% end %>
	</div>
</div>